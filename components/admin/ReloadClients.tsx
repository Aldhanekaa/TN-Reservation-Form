import { LoadingButton } from "@mui/lab";

export default function ReloadClients() {
  return (
    <LoadingButton variant="outlined" sx={{ marginRight: 1 }}>
      Reload Clients
    </LoadingButton>
  );
}
